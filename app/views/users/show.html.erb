<% provide(:title, @user.name + " (@" + @user.username + ")") %>
<div class="row">
  <aside class="span4">
    <section>
    	<span class="user-gravatar">
      	<%= gravatar_for @user %>
      </span>
      <span class="user-info">
	      <h3><%= @user.name %></h3>
	      <h4>@<%= @user.username %></h4>
	     </span>
    </section>
  </aside>
</div>